xdtoДокумент = сшпОбщегоНазначения.ПолучитьОбъектXDTO(Перечисления.сшпФорматыСообщений.XML, ОбъектСообщение.Body);

текТаблица = xdtoДокумент.Data.Последовательность();
колвоЗаписей = текТаблица.Количество();
Если колвоЗаписей > 0 Тогда
		новыйНабор = РегистрыСведений.Statistic.СоздатьНаборЗаписей();
		Для Инд = 0 По колвоЗаписей-1 Цикл
				xdtoСтрока = текТаблица.ПолучитьЗначение(Инд);
				новаяЗапись = новыйНабор.Добавить();
